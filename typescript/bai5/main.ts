import Rectangle from './Rectangle';

// Tạo đối tượng Rectangle
const rectangle1 = new Rectangle(5, 10);

// Tính diện tích 
const hinh1 = rectangle1.calculateArea();
console.log("diện tích hình đó :", hinh1);
